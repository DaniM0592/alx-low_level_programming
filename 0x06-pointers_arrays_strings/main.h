#include <stdio.h>
/**
 * Append src string to th dest string.
 * Overwrite the terminating null byte at the end of dest.
 * then add terminating null byte.
 *
 * Return Always 0.
 */
char *_strcat(char *dest, char *src);
